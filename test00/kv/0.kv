#:kivy 2.1.0

RootWidget:
    # import container
    container: container

    # fill the container
    BoxLayout:
        id: container
        orientation: 'vertical'
        padding: 0
        spacing: 3

        #Label:
        #    text: 'canvas'

        Image:
            source: 'app_logo.png'
            size: self.texture_size
