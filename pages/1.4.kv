#:kivy 2.1.0

RootWidget:
    # import container
    container: container

    # fill the container
    BoxLayout:
        id: container
        orientation: 'vertical'
        padding: 0
        spacing: 2

        Label:
            text: 'canvas4'
        Label:
            text: 'texto4'

        # Footer
        BoxLayout:
            orientation: 'horizontal'
            padding: 0
            spacing: 1
            size_hint_y: None
            height: '36dp'
            canvas.before:
                Color:
                    rgba: 0.203, 0.395, 0.641, 1  # 52,101,164 0x3465A4 Azul
                Rectangle:
                    pos: self.pos
                    size: self.size
            Label:
                text: '1.4 Tres líneas rectas'
            Button:
                size_hint_x: None
                width: self.height
                text: '<--'
                on_release: app.next_screen('1.3')
            Button:
                size_hint_x: None
                width: self.height
                text: ' '
                # on_release: app.next_screen('1.4')

